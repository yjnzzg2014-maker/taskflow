server:
  port: 8080

spring:
  application:
    name: taskflow

  datasource:
    url: jdbc:mysql://localhost:3306/taskflow?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: 1qazxsw2Fang007008
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

# JWT Configuration
jwt:
  secret: TaskFlowSecretKey2024ForJWTTokenGenerationMustBeLongEnough256Bits!
  expiration: 86400000
  refresh-expiration: 604800000

# File upload
file:
  upload-dir: ./uploads
  max-size: 10MB

# CORS
cors:
  allowed-origins: http://localhost:5173,http://localhost:3000,https://task.stjjkf.xyz,https://task-api.stjjkf.xyz

logging:
  level:
    com.taskflow: DEBUG
    org.springframework.security: DEBUG
